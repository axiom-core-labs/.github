name: Chore
description: Maintenance work — cleanup, dependency bumps, small non-feature changes.
title: "[Chore]: "
labels:
  - chore
body:
  - type: markdown
    attributes:
      value: |
        Write this for an agent, not a human. Be concrete and testable — objective, scope,
        and acceptance criteria should leave no ambiguity.

  - type: textarea
    id: objective
    attributes:
      label: Objective
      description: One sentence. The desired end state.
    validations:
      required: true

  - type: textarea
    id: context
    attributes:
      label: Context
      description: Why this matters. Link related issues or PRs.
    validations:
      required: false

  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: Files, directories, or repos the agent may touch.
    validations:
      required: true

  - type: textarea
    id: out-of-scope
    attributes:
      label: Out of scope
      description: What the agent must NOT touch or change.
    validations:
      required: false

  - type: textarea
    id: acceptance-criteria
    attributes:
      label: Acceptance criteria
      description: Checkbox list. This is the agent's definition of done.
      value: |
        - [ ]
        - [ ]
    validations:
      required: true

  - type: textarea
    id: verification
    attributes:
      label: Verification
      description: Exact commands to run, e.g. `npm run build`, `npm run lint`, `npm test`.
      render: shell
    validations:
      required: false

  - type: checkboxes
    id: constraints
    attributes:
      label: Constraints
      description: Select all that apply.
      options:
        - label: No new dependencies
        - label: No schema changes
        - label: No changes to CI / workflow files
        - label: Keep bundle size flat
    validations:
      required: false

  - type: dropdown
    id: deliverable
    attributes:
      label: Deliverable
      options:
        - PR only
        - PR + preview deploy
        - Investigation, no code
    validations:
      required: true

  - type: dropdown
    id: autonomy
    attributes:
      label: Autonomy
      options:
        - Agent may proceed unattended
        - Ask before implementing
    validations:
      required: true
