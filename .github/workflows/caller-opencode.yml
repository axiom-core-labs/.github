name: OpenCode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  opencode:
    uses: axiom-core-labs/acl-cicd/.github/workflows/opencode.yml@main
    secrets: inherit
